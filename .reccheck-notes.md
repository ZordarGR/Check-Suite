# RecCheck — working notes

A hidden dotfile, deliberately. It is not in the nine files `app.asar` packs, so it never
reaches an installed copy; it exists so a fresh session can pick this up without
re-deriving any of it.

Owner: Dimitris (`ZordarGR`), night auditor, Kernos Hotel, Malia. Repo `ZordarGR/Check-Suite`.
The work machine is not his, so nothing may be installed or changed on it — every build,
release and test happens in the cloud container.

Last release: **1.17.21**.

**Write these notes as reference, not prose.** Bullets, tables, short declaratives. Keep
every fact — the dates, the numbers, the reasons a guard exists — and cut the narration.
He asked for this on 02/09: *"try to make the notes a little less... essay-like"*. If a
paragraph explains how a conclusion was reached rather than what it is, it does not
belong; state the conclusion and what would overturn it.

---

## 1. What the tool is

An Electron 43.2.0 app that audits a night's POS receipts against protel's
`checkcharge1` report (`ΕΛΕΓΧΟΣ ΤΜΗΜΑΤΩΝ BY ROOM`, saved as `.oxps`). Two tools in one
page:

* **Department Check** — the receipt audit. Loads the `.oxps`, lists receipts per
  department, you search each paper serial and tick it. Corrections, manual entries and
  a printed sheet for reception come out the other end.
* **Tax Check** — a separate screen, cross-references a **rate list** (arrival/departure
  dates per room) against a **tax report**. Only run on nights where the main courante
  did not balance.

Alongside it: a nightly checklist with a desktop overlay, room nicknames, a watchlist,
per-user profiles, and `rc-tbind.exe` — a native helper that binds mouse buttons and key
combinations to protel shortcuts.

### The τ shortcut, the thing that has cost the most time

protel's invoice list jumps to the `ΤΕΛΟΣ ΑΝΘΕΚΤΙΚΟΤΗΤΑΣ` entry when you press the Greek
letter τ. It ignores an injected Unicode τ — it wants a real keystroke. So the helper
presses the **physical T key while Windows believes the keyboard is Greek**, then puts
the layout back. Everything hard about it follows from that.

---

## 2. Layout of the repo

```
app/index.html          the whole app: 4 <script> blocks, each an IIFE
                        #parser, #app (department check), #jszip, #taxapp (tax check)
app/main.js             Electron main: windows, tray, hotkeys, helper lifecycle, IPC
app/preload.js          contextBridge: reccheckShortcuts / reccheckOverlay / reccheckFiles …
app/overlay.html        the desktop checklist overlay
app/updater.js          self-update: full installer or light html
app/files.js            the reports-folder browser + config store
app/tbind.cs            source of the helper
app/rc-tbind.exe        the built helper (checked in)
app/build-helper.sh     THE ONLY sanctioned way to build it
app/build-check.py      metadata guard, see §5
app/bcl-allow.txt       names the guard allows
Departments Check.html  a byte-identical COPY of app/index.html, see §4
update/latest.json      the update manifest clients poll
dist-win64/parts/       the installer, split into 20 MB pieces
.github/workflows/release.yml
```

`app/index.html` and `Departments Check.html` **must be identical**. The release workflow
refuses to publish otherwise.

---

## 3. How a release works

The workflow fires on a push touching `dist-win64/parts/**` or the workflow file. It
reassembles the parts, checks the sha, and republishes the tag.

`gh release create --latest` without `--target` tags the **default branch**, so a release
cut from a feature branch points at the wrong commit until it is merged. Merging fixes
it. This is why the workflow runs twice per release and that is fine.

### Building the installer (in the container)

```
scratchpad/stage/            the unpacked Electron payload, byte-for-byte from 1.11.5
scratchpad/reccheck.nsi      reconstructed NSIS script (NSIS 3.09-4, not electron-builder)
```

1. bump `app/package.json`, `APP_VERSION` in `app/index.html`, `DisplayVersion` in the .nsi
2. `cp app/index.html "Departments Check.html"`
3. copy the nine files into `scratchpad/pack/`, `npx asar pack pack stage/resources/app.asar`
   (files.js, index.html, main.js, overlay.html, overlay-preload.js, package.json,
   preload.js, tray.ico, updater.js — 1.17.10 briefly made it eleven; 1.17.11 moved the
   Caps Lock icon into the helper and took it back to nine)
4. `cp app/rc-tbind.exe stage/resources/`
5. `makensis reccheck.nsi` — ~5 minutes, run it in the background. **Wait on the process,
   not on the call.** Backgrounding it a second time (`nohup … &` inside an already
   backgrounded call) reports exit 0 the instant the shell forks, and the half-written
   `RecCheck-Setup.exe` sitting there looks like a finished 7 MB build. Poll `pgrep -x
   makensis`. A real one is ~93 MB; the log's last line is `Total size:`.
6. **unpack the result and verify it** before splitting — `7z x` the installer, then
   `npx asar extract` the `resources/app.asar` inside it and check `APP_VERSION`, the file
   count, and that the page is byte-identical to `app/index.html`. `strings` proves
   nothing: the payload is solid LZMA, so nothing inside it is greppable, and neither is
   the `DisplayVersion` the .nsi writes.
7. `split -b 20971520 -d -a 3` into `dist-win64/parts/`
8. update `VERSION` + `SETUP_SHA256` in the workflow and `update/latest.json`

### Two kinds of update

* `type: "full"` — 90 MB installer. Needed whenever `main.js`, `preload.js`, the helper or
  anything outside the page changes.
* `type: "html"` — ~370 KB. Only `Departments Check.html`. `engine` names the minimum
  engine version; an older engine takes the full path instead and can never strand itself.

An html update is promoted only if its version beats the installed `pkgVersion`, so
`APP_VERSION` in the page and `version` in the manifest have to move together.

---

## 4. Landmines — every one of these cost real time

* **ASAR integrity.** The shipped exe carries an `/INTEGRITY/ELECTRONASAR/` resource, but
  the fuse `embedded_asar_integrity_validation` is **DISABLED** (sentinel
  `dL7pKGdnNz796PbbjQWNKmHXBZaB9tsX`). Repacking `app.asar` is therefore safe. Verify
  before assuming.
* **Mono's BCL is bigger than .NET Framework's.** `mcs` compiles against Mono; the helper
  runs on .NET Framework 4.8. `body.Split(',')` bound to `String.Split(char,
  StringSplitOptions)`, which 4.8 does not have — the JIT resolves every token in a method
  before running it, so the whole of `ParseBind` threw `MissingMethodException` on first
  call and the helper died 53 ms in with exit `0xE0434352`. Hidden for three versions
  because a second bug meant nothing was ever bound, so the helper never started. **This
  is what `build-check.py` exists to prevent.**
* **A test that lifts a function by text proves that function, not its caller.** The
  movements panel showed nothing for two versions. A harness lifted `roomMoves` out of
  the page, drove it with a real ledger, produced correct pills, and I reported the panel
  was not broken. It was: `renderMoves` called `liveReceipts()`, which does not exist
  anywhere — the real name is `checkableList()` — so it threw a `ReferenceError` after
  appending its title and before the first pill, and `stage()` swallowed it. His own
  DEBUG report said `43 pills` while the panel was blank; that contradiction was the
  evidence and I did not read it. **Lift the function that is actually called, or check
  the scope.** The scope check below finds this in a second.
* **The tax half is namespaced; the audit half is not.** Everything under `#taxScreen`
  is prefixed, but the audit's own chrome is written on bare element selectors — `header`,
  `header h1`, `.btn`, `.acc`. The tax page has a `<header>` and an `<h1>` of its own, so
  it had been rendering them as the audit's sticky, blurred, bordered, amber-monospace,
  `white-space:nowrap` app bar since the two halves were merged. `#taxScreen header h1`
  only overrode the properties it happened to re-declare; the rest leaked. Between 901 and
  ~1090 px — where the help panel takes 300 px and leaves under 600 — that pushed the whole
  page past the window. **A rule under `#taxScreen` must state every property the bare
  selector sets, not just the ones being changed.** `.acc` still inherits the audit
  accordion's panel background and border; verified harmless on screen, so left alone.
* **A truncated diagnostic produces a truncated diagnosis.** `movesReport` printed
  "stays near this night" and cut it at 40 rows. On 02/09 that cut at room 153, and the
  conclusion drawn from it — that every pill above 153 must come from the name-change
  deduction — was right about the rooms it could see and wrong to be stated as the whole
  cause. Removing the deduction did not stop the phantom departures, because the OTHER
  source was never visible in the report: **stays whose recorded departure date is simply
  out of date.** Nothing is deduced now; a ledger entry written by an old rate list just
  keeps saying what that list said. 1.17.21 prints every pill with the date of the list
  that wrote it and counts how many are older than the newest, which is the difference
  between the two causes on one line. **Never diagnose from a report that elides.**
* **A name change is not a date. Third time.** 30/08: a rule that ended a stay when a
  room charged under an unrecognised name made 83 phantom departures. It was removed from
  the ledger writer — and the same inference survived in `roomMoves`, adding a dashed
  TURNOVER pill for any room whose department-report name differed from the cached one.
  On 02/09 the last rate list loaded was 31/08, so every real changeover of 01/09 and
  02/09 read as a name change: rooms **65, 94, 124, 129** — with no arrival or departure
  on that night in the ledger at all — were all claimed to have left and been re-let.
  Proved from his own DEBUG dump: those rooms are pills, and they are absent from the
  same dump's list of stays within three days of the night. **Dashed does not help — a
  turnover pill claims a departure.** Gone in 1.17.17. `ROOMS[].movedOn` is still stamped
  and now read by nothing.
* **A room number is not a stay.** The receipts dot on a departure pill was built from
  `effRoom(r)` alone, so any live receipt for that room number lit it. The moment a room
  turns over — and with no rate list since the arrival, a turned-over room still shows as
  a plain departure — the dot lands on the NEW occupant's receipts, which is paper he
  must not throw away. It matches on the departing guest's name now, through
  `leavingIndex()`/`isLeaving()`, the same index behind the red mark in the search
  results, which was specified against exactly this mistake.
* **`display:none` in a stylesheet is not cleared by `style.display = ""`.** That only
  removes the inline override. `#notThereBtn` was never once visible. Toggle a class.
  And **assert on `getComputedStyle`, never on `el.style.display`** — the latter reads
  `""` precisely when the rule is hiding the element.
* **raw.githubusercontent caches the manifest ~5 minutes per regional edge.**
  `cache: "no-store"` does not bypass it; a unique query string does. This hid two
  releases.
* **An `HKL` is `(device << 16) | language id`.** Comparing whole handles reports a
  working layout hop as failed. Compare the low word.
* **`GetKeyboardLayout(0)`** reports the *calling* thread's layout. A thread id of 0 made
  a hop that never happened look successful.
* **`SendInput` is not synchronous.** Checking the queue immediately after it returns
  reads empty because the key has not arrived yet, not because it was consumed.
* **The two page copies drift.** `Departments Check.html` was left at a pre-1.16.1 state
  and would have shipped a downgrade. The workflow now `cmp`s them.
* **The script blocks are IIFEs** — nothing is on `window`. To test, inject a probe before
  the final `})();` on a copy.
* **jsdom needs an http origin** for `localStorage`; on `file://` it throws.

---

## 5. The build guard

`app/build-helper.sh` compiles and then runs `app/build-check.py`, which reads the
compiled assembly's `#Strings` heap and fails on any name not in `app/bcl-allow.txt`.

Adding a name to that file **is** the moment to confirm the member exists in .NET
Framework 4.8, because nothing else in the build will tell you. It is a name-level check,
not a signature-level one — a tripwire, not a proof. It also flags our own new method
names, which is the accepted cost.

Reintroducing the `Split` bug makes it exit 1 naming `StringSplitOptions`.

---

## 6. Testing, since none of this can run on Windows here

* **jsdom** against a probe-injected copy of the real page — this is how every renderer
  change has been verified. Harnesses live in the scratchpad (`h-all.html`, `h-led.html`, …).
* **The real `main.js` with electron stubbed** — `Module._load` intercepts `require("electron")`,
  the stub captures `ipcMain.handle` and `globalShortcut.register` so the actual handlers
  and hotkey callbacks can be driven.
* **The helper under Mono** — `ping`, argument parsing and the crash reporter all work on
  Linux. Anything touching `user32` does not, and fails at `GetCurrentThreadId`.
* **Scope-check before believing a render test.** Thirty lines, rebuilt in the scratchpad
  when needed: slice out the IIFE that holds the renderer, collect every
  `function`/`const`/`let`/`var` declared in it, then report any `name(` call site that
  resolves to nothing. A silent `ReferenceError` inside a `stage()` looks exactly like
  "there was nothing to draw". It is not committed — see the queue.
* **Sweep layout with a real browser, not by reading CSS.** `playwright-core` and the
  Chromium at `/opt/pw-browsers` drive a probe-injected copy of the page (`window.__t`
  exporting the dialog openers and `showScreen`). Open each dialog and each screen at
  420/640/760/909/1280/1600 and compare `scrollWidth` with `clientWidth` on `#modal` and
  on `documentElement`. That is what found both 1.17.16 bugs, and what proved the other
  nine dialogs were fine. Exclude `#drawer` — it is parked off-screen by design.
* **The installer is always unpacked and checked before splitting.**

---

## 7. Version history, and what each one was actually about

| | |
|---|---|
| 1.11.6 | v3 helper: verify the layout hop instead of sleeping 90 ms and hoping |
| 1.12.0 | PROTEL SHORTCUTS menu, Alt+F4 bind, keyboard triggers, per-profile bindings |
| 1.12.1 | home greeting reset to REC·CHECK — `refreshProfileName()` never called at boot |
| 1.12.2 | checklist reordering with ▲▼ (severity had been driving the sort) |
| 1.13.0 | diag mode + the HKL language-id fix + target the focused child control |
| 1.14.0 | overlay completion animation, click-through, done reminder; CDN cache-buster |
| 1.15.0 | τ hops via Win+Space; keystroke run; modifier+button combos |
| 1.16.0 | fixes 1.15.0 destroying bindings (`BTN:<mods>-<btn>` vs a parser reading `/BTN:(\d)/`) |
| 1.16.1 | mouse hook stopped discarding driver-injected side buttons; helper status line |
| 1.16.2 | **the `Split` crash**; the build guard; the helper reports its own crashes |
| 1.16.3 | `refresh()` stages isolated; `uiFault()`; the two page copies synced + guarded |
| 1.16.4 | a finished night answers the same way whichever overlay key asks |
| 1.16.5 | τ: wait for the key to arrive and be taken before restoring the layout |
| 1.16.6 | add-by-hand form names the room's guest; the prompt says what it is |
| 1.16.7 | **that prompt had never been visible** — the CSS rule above |
| 1.16.8 | manual entries count everywhere: totals, search, watchlist, duplicate guards |
| 1.16.9 | room movements panel beside the departments list, five to a line |
| 1.16.10 | movements become a forward-looking ledger, not a per-night snapshot |
| 1.16.11 | the nightly department report keeps that ledger honest |
| 1.17.0 | every real τ press records what it did; hidden DEBUG menu |
| 1.17.1 | a rate list whose header date would not parse recorded **nothing**, silently |
| 1.17.2 | the shortcuts belong to protel: a trigger only fires while protel is in front |
| 1.17.3 | the τ presses its own Enter — the intermittency was a person out-racing protel |
| 1.17.4 | the home screen fits the window; the τ's wait drops to 50 ms and becomes settable |
| 1.17.5 | the checklist flows into columns; dialogs stop growing past the window |
| 1.17.6 | Alt+N — protel's "new" on the passport screen — joins the bindable actions |
| 1.17.7 | a nickname follows a guest who changes rooms |
| 1.17.8 | room pills group by colour under DEPARTED / TURNOVER / ARRIVED headings |
| 1.17.9 | the ledger records the dates protel holds and deduces nothing |
| 1.17.10 | Caps Lock shows on screen — the keyboard has no light and this PC draws nothing |
| 1.17.11 | the helper stands on its own: starts with Windows, Caps Lock always, shortcuts only alongside RecCheck |
| 1.17.12 | (html) a receipt on a room leaving today is marked red — matched on the DEPARTING guest's name only |
| 1.17.13 | moves stop counting as arrivals; departures with receipts get a dot; the τ's real costs cut; the dialog uses the window; the ledger can be started again |
| 1.17.14 | the read-only window scan ships, and reports its own cost |
| 1.17.15 | (build 10) the movements panel was crashing and drew only its title; the working night turns at 03:30, the shift still ends at 07:00 |
| 1.17.16 | (html) a layout sweep: the DEBUG dialog fitted its own buttons, and the audit's header styling stopped leaking into the Tax Check page |
| 1.17.17 | (html) the pills stop deducing a turnover from a name change; the receipts dot matches the departing name, not the room |
| 1.17.18 | the zero-touch half of the live protel read (helper v13); the 07:00 overlay reset stops summoning the overlay |
| 1.17.19 | (html) legacy mode: a settings toggle that hides the Rate Check slot |
| 1.17.20 | (html) legacy mode moves to OPTIONS, where it belongs |
| 1.17.21 | (html) DEBUG lists every pill and the rate list that wrote it, instead of a truncated 40 |

---

## 8. Where things stand

### Two clocks, since 1.17.15

His words: *"the overlay and task list will still reset at 7, that does not change,
however the working night will change when the clock hits 3:30 am"*.

| | boundary | constant |
|---|---|---|
| working night — which night a report is for | 03:30 | `NIGHT_ROLLOVER_H = 3.5` |
| checklist ticks + desktop overlay | 07:00 | `SHIFT_ROLLOVER_H = 7` |

The working night is protel's date. It drives `bnKey()`, `bnKeyOf()` (the TONIGHT badge
in the file browser), `nightsAgoKey()` / `relNight()`, the welcome line, and `bnk()` in the
tax half — a **separate IIFE**, so the 3.5 is written out there a second time. Change one,
change both.

The shift is his, not protel's, and ends at 07:00. `clNightCheck()` uses `shiftKey()`.

`clNightCheck` clears **only forward** (`k > prev`). Between 03:30 and 07:00 the key the
old single boundary wrote reads one day ahead of the new shift key, so a plain `!==` would
have wiped his ticks once, mid-shift, on the upgrade itself.

Not handled: the two DST nights. The boundary is `Date.now() - h hours` read in local
time, so on the night the clock moves it lands an hour early or late, once. The old
single boundary behaved the same way. Nobody has asked.

### rc-tbind stands on its own (v10, 1.17.11)

His words, do not re-couple these: *"make rc-tbind.exe to work on its own, however
protel's shortcuts will work only when both rc-tbind.exe and reccheck.exe are running
simultaneously"* / *"the caps lock indicator will work regardless of reccheck running or
not"*.

| | gated on RecCheck? | hook |
|---|---|---|
| Caps Lock icon | no | keyboard, always installed |
| protel shortcuts | yes | mouse, installed only while RecCheck runs |

* `hostUp` = `Process.GetProcessesByName("RecCheck")`, polled every 2 s on the old
  watchdog thread. Nothing read from RecCheck, nothing sent to it.
* `SetHost` unhooks the mouse when RecCheck goes — so a machine with the tool closed
  carries no mouse hook at all, the same scope as before this change. Keyboard triggers
  need an explicit `hostUp` check because that hook stays up.
* `SetHost` / `LoadBinds` run on the **loop thread** — an LL hook belongs to the thread
  that installs it, and only that one pumps.
* Bindings: `%LOCALAPPDATA%\RecCheck\rc-tbind-binds.txt`, one token per line, the same
  words that used to be arguments. `ParseBind` untouched. Re-read on write-time change.
* Login entry `HKCU\...\Run\RecCheckHelper`. Verbs `install` / `uninstall` / `stop` /
  `status`. `stop` stands down without touching the entry — RecCheck uses it while
  listening for a button to bind. The NSIS uninstaller runs `uninstall` BEFORE deleting
  the exe.
* Icon is raw GDI, not WinForms/System.Drawing — that would put a large BCL surface
  behind a name-level tripwire. Two passes (white silhouette, then dark glyph with a null
  pen) because GDI strokes over its fill and would close the counter of the A.
* `CreateCapsWindow` failing is not fatal: no icon, but `PostUi` falls back to
  `PostThreadMessage` and the shortcuts carry on.

**Unverified (no Windows here):** the layered window rendering and the fade; the
`GetKeyState` seed of `capsOn` — if the FIRST flash after a start says the wrong word,
invert it; `hostUp` against a real RecCheck starting and stopping.

**1.17.10 superseded:** it drew the icon inside Electron and died with RecCheck. Removed.
`main.js` has a one-time `bootMigrated` flag so anyone who had it on gets the login entry
written once.

### "Leaving today" mark on a search result (1.17.12)

A receipt on a room due out today has to be settled before they walk. Whole match bar goes
red (his choice), LEAVING TODAY pill, red arrow by the checkbox.

**The name is the safety condition.** *"if i have two receipts for the same room number
but different names then the name that has a departure date of today is the one i want
marked, dont accidentally mark the arrival receipts (i rip those and throw them, so
please, careful)."* A room leaving today may already be re-let to an arrival; marking that
receipt would make him destroy paper he needs. `isLeaving(room, name)` needs BOTH a ledger
entry whose departure == tonight AND `sameName()` — strict, never the tolerant one.

`leavingIndex()` built once per search, not per card (reads localStorage).

Tested by `scratchpad/leaving.js`, 18 cases against the shipped functions. First case is
the one that matters: same room, SMITH leaving today and JONES arriving today, only
SMITH marked. Near misses, blank names, wrong dates, unknown room, empty ledger and an
unreadable report date all mark nothing.

**Trust it.** It shipped with a caveat that it rides on phantom departures. That was
wrong — departures have been sound since 1.17.9; arrivals were the phantom half. Do not
re-add the caveat.

## QUEUE — what is left

* **Filter titleless window openings out of the watch log?** Two of the first nine lines
  were top-level `Static` windows with no caption, which say nothing. Dropping them makes
  the log readable; keeping them means the log is exactly what Windows reported. **His
  call — not built.**

* **Should a name change show as anything at all?** It is real information — the
  department report says the name under a room is not the name that was there before —
  it just is not a departure. It could have its own kind and its own colour, saying only
  "the name changed", asserting no dates. The CSS and strings for the dashed `guess` pill
  and its footnote are still in the page, unused, for exactly this. **His call — not
  built.**

* **Commit the test harnesses?** They live in the scratchpad and die with the container,
  so §6 keeps citing files that no longer exist. Committing the scope check and the
  movements-panel harness would make them real, at the cost of test files in a repo that
  has never carried any. **His call — not built.**

Rule 5 as amended 02/09: ideas wait for an explicit go, **bugs are fixed on sight without
asking**. If it is unclear which, it is a feature — ask.

**Nothing queued for build.** Open items are all questions for him or measurements only he
can take:

| open | what settles it |
|---|---|
| does the scan cost protel anything? | the `N message(s) … took T ms` line from a real run |
| does the τ feel different since 1.17.13? | his hands, or `rc-tbind-tau.log` |
| was the 02/09 τ lag a real regression? | τ log entries from 29-31/08 — the file keeps several nights |
| why is the movements panel empty (02/09)? | DEBUG → *Why the room pills say what they say* |

**NOT implemented, and the data does not exist for it:** time-based departures — a
departure disappearing once its time has passed. The Rate Check carries **no time column
at all**. Verified on his real 01/09 file, the eight columns are: room · name · arrival
(x240) · departure (x312) · occupancy (x389) · rate (x481) · board (x544) · agent (x576).
Times would have to come from the printed departure list or the live feed, neither of
which the tool reads. It belongs to the live-feed design below, which is design only.

### The window scan shipped in 1.17.14 — and the terms it shipped on

He decided on 02/09, in his words: *"let's build it, better in a live environment than
trying to test it under controlled scenarios, if it makes things worse we purge that part
of the code."* So this is a live experiment with an explicit exit, and the exit needs
evidence — which is why the report ENDS WITH ITS OWN COST: how many messages it asked of
protel and how long the sweep took. Do not remove that line; it is the whole basis on
which the feature is kept or pulled.

**What "read-only" means here, in his terms.** He defined it the Greek way: do not ALTER.
By that test:
* data and variables: **untouched**. Every message is a getter — WM_GETTEXTLENGTH,
  WM_GETTEXT, LVM/TVM/CB/LB_GETCOUNT. Nothing writes. Nothing is sent that could change
  what protel shows.
* influence: **small but not zero**. Asking a control a question runs code on protel's UI
  thread. On a standard control that handler is Windows' own common-controls code and
  protel never sees it; on anything protel subclasses or owner-draws, protel's code runs.
  All calls carry SMTO_ABORTIFHUNG with 120/200/250 ms timeouts.
* the structural half — EnumChildWindows, GetClassName, GetDlgCtrlID, GetWindowRect,
  IsWindowVisible — sends nothing at all and costs protel literally zero. **If the cost
  ever has to go, that half can stay and still report the control tree.** That is the
  purge path: drop CtrlText and RowCount, keep ScanOne's other four reads.

**The cost scales with CONTROLS, not rows.** A list holding ten thousand rows is one
message and one integer. A dialog carrying two hundred fields is two hundred. Cap 500.

**Not measured, and it should be:** how long a real sweep takes on his PC. The report
prints it; read the number before believing any estimate, including mine.

### A room move is INDISTINGUISHABLE from an arrival in the report — measured, 01/09

His real Rate Check for 01/09 was parsed here with the shipped `parseRate`. He identified
rooms 148, 263 and 535 as guests he had MOVED that day. Their rows:

```
[ARRIVAL] 112   112 | name | 01/09/26 | 12/09/26 | 2000 | 196,20 | HB | SCHAUINSLAND
[MOVED  ] 148   148 | name | 01/09/26 | 04/09/26 | 3000 | 344,40 | BB | TUI
[MOVED  ] 263   263 | name | 01/09/26 | 07/09/26 | 2000 | 188,00 | BB | ITS Coop
[STAYING] 177   177 | name | 29/08/26 | 05/09/26 | 1000 | 160,72 | HB | TUI SUISSE
```

Same eight columns, same x positions, arrival dated today. Occupancy, rate, board and
agent say nothing. **protel closes the old room's stay and opens a NEW one on the new room
dated from the move.** So "arrival dates follow the guest, not the room" holds while a
guest stays put and NOT through a move — he had it the other way round, and this is the
fact that explains the whole thing. 29 arrivals shown = 26 real + 3 moves.

### The row-merge diagnosis was WRONG — refuted by measurement, 01/09

Six agents and an adversarial pass concluded that `toRows` merges two printed lines and
`parseRate` then takes the room from one and the dates from another. His real file kills
it. Do not resurrect it:

* **every data row carries exactly 2 dates.** Never 3, in 233 rows.
* **the date columns are fixed**: arrival at x=240, departure at x=312, all 233 rows.
* **no row contains two room cells.** Nothing merged.
* **line spacing is ~19 units** against `toRows`' `<6` threshold — nowhere near merging.
* 227 rooms, 227 rows, **zero reversed or missing date pairs**.

Binding the dates to their columns — the fix that was going to be written — would change
NOTHING, because column order and print order already agree.

Also found: six rows are silently dropped, all 4-digit house accounts
(`9005 9040 9601 9604 9605 9608`, most running 18/04→10/11). The `/^\d{2,3}$/` room filter
excludes them. Correct, but they are invisible to the tool.

Also: **no room in a Rate Check departs on the day of that list.** It is a pre-EOD
in-house list, so tonight's departures were learned from YESTERDAY's list. By design, but
load-bearing for the 1.17.12 "leaving today" mark, which can only fire on rooms an earlier
night taught the ledger.

### Which half of the ledger is wrong — settled by him, 01/09

**Departures: correct, and have been since 1.17.9.** *"they were always okay after the
first fix."* Do not go hunting there.

**Arrivals: the phantom ones.** A guest who arrived 28/08 and moved rooms on 01/09 showed
as an ARRIVAL tonight, while the rate list plainly prints 28.08 against the new room —
because arrival dates in protel follow the GUEST, not the room.

That split is itself the strongest evidence for the row-merge diagnosis below, and it was
not what the diagnosis was fitted to: a merged row gives the room its NEIGHBOUR's arrival
(tonight, so a phantom ARRIVED) while its own real arrival is demoted into the DEPARTURE
slot, where it is a past date and fires nothing. Phantom arrivals with clean departures is
the mechanism's own signature. The mirror orientation would produce a false departure
instead, and he does not see those — which says the orientation is fixed, not random.

### Movement counts — status after 01/09, mostly closed

Was: "37 departures where the desk counts 33", plus a table of arrival dates that looked
wrong on 30/08 (78 read 28.08 for 26.08; 106 read 26.08 for 24.08; 79 out by one).

What is now known:

* **Departures: fine.** His testimony — *"they were always okay after the first fix"*,
  i.e. since 1.17.9 removed the truncation. Do not go hunting there.
* **Inflated arrivals: explained and fixed.** Room moves. protel prints one as a new stay
  on the new room dated today, indistinguishable from an arrival. 1.17.13 detects them.
* **`parseRate` is correct on the file we have.** His real 01/09 Rate Check: 227 rooms,
  227 rows, exactly 2 dates per row, columns pinned at x=240/x=312, zero reversed or
  missing pairs. The row-merge theory is dead (below).
* **Still unexplained:** the 30/08 date table. Never reproduced — it was measured against
  a night whose `.oxps` we do not have. Do not assume it is real until it is seen again in
  a file.

Ledger reset shipped in 1.17.13 (PROTEL SHORTCUTS → Room movements memory), so there is
now a way back from a bad ledger.

### The live feed — the zero-touch half is BUILT (1.17.18, helper v13)

**What exists now.** `SetWinEventHook(EVENT_OBJECT_SHOW..HIDE, …, WINEVENT_OUTOFCONTEXT |
WINEVENT_SKIPOWNPROCESS)` in `rc-tbind.exe`, scoped to protel's pid. It records that a
protel window opened or closed, when, its class and its caption, to
`%LOCALAPPDATA%\RecCheck\rc-tbind-watch.log`. DEBUG → *What protel opened tonight*
reads it.

**It sends protel nothing. Not "little" — nothing.** Out-of-context means the callback
runs in OUR process and Windows marshals the event; nothing is injected. `GetClassName`
answers from the window class, kernel side. `GetWindowText` on a **top-level** window of
another process returns the caption Windows already cached and does not send WM_GETTEXT —
true only for top-level windows, which is why the callback drops anything with an
ancestor (`GetAncestor(h, GA_ROOT) != h`). That guard is load-bearing; do not relax it.

**It runs on its own thread.** Never the loop thread. A late low-level hook callback gets
its hook torn down by Windows, the mouse and keyboard hooks live on the loop thread, and
protel's shortcuts are the critical half of the program. `WatchLoop` pumps its own queue
with PeekMessage on a 100 ms tick.

**With no `focus=` target configured it watches nothing at all** — `ProtelPid()` returns 0
and no hook is installed, rather than logging the whole desktop.

**Retention, per his decision: tonight only, cleared at 07:00.** The file names the shift
it belongs to. A restart mid-shift keeps that night's lines; the first write of a new
shift throws the old ones away. The boundary is `SHIFT_ROLLOVER_H`, mirrored in
`ShiftKey()` in tbind.cs — change one, change both.

**The first real run, 03/09 06:32 — what protel is actually made of.** Nine lines, from
the few minutes between installing 1.17.18 and the 07:00 wipe. Small, and it settles more
than the size suggests:

```
06:32:33  OPEN   class="OWL_Window"                        title="Inhouse"
06:32:33  CLOSE  class="OWL_Window"                        title="Inhouse"
06:32:36  OPEN   class="#32770"                            title="Report Selection"
06:32:37  CLOSE  class="#32770"                            title="Report Selection"
06:32:39  OPEN   class="Afx:10000000:b:10003:6:163d0c3b"   title="Departure List By Room"
06:32:39  OPEN   class="Static"                            title=""
06:32:40  OPEN   class="Static"                            title=""
06:32:48  OPEN   class="OWL_Window"                        title="Occup."
06:32:48  CLOSE  class="OWL_Window"                        title="Occup."
```

* **The hook works on his machine.** Scoped to protel, correct shift stamp at 06:32
  (still the 02/09 shift — the 07:00 boundary behaving), captions arriving. Every claim
  about this half except "it runs" was already verified in the container; this was the one
  that could not be.
* **protel is not one custom-painted canvas.** Three real window classes, all standard:
  `OWL_Window` is Borland's ObjectWindows Library, `#32770` is the standard Windows
  DIALOG class, and `Afx:…` is MFC. Verified, not inferred — those three prefixes are
  unambiguous. That protel is a Borland core with MFC parts bolted on is the inference.
* **Therefore its windows are real windows**, which is what the 1.17.14 scan needs to be
  able to enumerate anything at all. It also means `#32770` dialogs — "Report Selection",
  and by extension the date-change dialog — are detectable by caption alone, with nothing
  sent to protel.
* **`Departure List By Room` is a real top-level MFC window that opened and stayed open.**
  That is the window carrying the times the live feed wants and the Rate Check does not
  have. Whether they can be read is the next question, and the scan answers it without
  guessing — **run it with that window in front.**
* **Noise, not defects:** the two titleless `Static` entries are genuine TOP-LEVEL windows
  (a child could not pass the `GetAncestor` guard), and they carry nothing. `Inhouse` and
  `Occup.` opening and closing in the same second are protel reusing transient windows.
  Filtering titleless openings is a judgement call, not a fix — it is queued, not done.
* **Not proved by this:** how much a full shift produces. Nine lines is the tail of one,
  not a sample of one.

**The scan on the Departure Report, 03/09 06:36 — the cost, and the answer.**

```
front: PROT32 | FO C:\PROTEL\PROTEL.INI | Kernos Hotel … protel Hotel Management Suite 2024
56 child window(s)
98 message(s) asked of protel, whole sweep took 0 ms
```

* **The cost question is settled and the scan stays.** 98 messages, 0 ms. `0` means below
  `Environment.TickCount`'s resolution — under about 16 ms — not literally nothing. Either
  way it is free at the desk. The purge path in the section above is not needed.
* **protel is `PROT32`, top-level class `FO`, an MDI app** (`MDIClient`). That is the
  needle the watcher resolves and it evidently matches.
* **THE ANSWER: `SysListView32 id=24444  ROWS=17`.** The departure list is a real Windows
  list-view that reports its own row count, with a `SysHeader32` for columns, and the
  status bar next to it says `"Rm.: 17"` — they agree. protel does not paint its lists.
  Everything the live feed wants rests on this and it holds.
* **The window title carries the night**: `"Departure Report for 03/09/26"`. Free — a
  caption, no message.
* **The Departures filter dialog enumerates completely**: `Edit id=102` is the date
  (`"03/09/26"`), `104` room, `109` name, `111` companion, `112` first name, `113`
  Sc/TA/Cm/Gp; `Button id=1` OK, `2` Cancel, `11029` Print, `11107` "Include departed
  guests". Every field has a stable control id.
* **protel embeds Internet Explorer** — `AtlAxWin` → `Shell Embedding` → `Shell DocObject
  View` → `Internet Explorer_Server`, full window. So the aside in `tbind.cs` by the τ's
  `TranslateMessage` ("an embedded browser control (protel's list is one)") is half right:
  the browser is real, but the DEPARTURE list is not it. Left alone rather than corrected,
  because touching that comment costs a full installer for a parenthesis.

**The next decision, and it is his: how to read the ROWS.** Counting them is free and done.
Reading their text from another process is not, and there are two mechanisms:

* `LVM_GETITEMTEXT` needs the `LVITEM` and its buffer to live in **protel's** address
  space — `VirtualAllocEx` + `WriteProcessMemory` + `ReadProcessMemory`. It reads only,
  but it allocates and writes inside protel's process. Against his standing rule about
  protel's operation, that is a real question, not a formality.
* **UI Automation / MSAA** asks Windows' own proxy provider for the control instead. No
  allocation in protel, no memory written. It still causes messages to reach the control,
  so it is not zero-touch — but it is the mechanism that does not write into protel.

Put both to him plainly and let him decide. Do not fold it into a recommendation.

**Still to come, and NOT built:** reading fields. That is the half that costs protel
something, and the half where his second decision applies — record the protel user where
protel shows it. Nothing on the zero-touch half exposes a user, so there is nothing to
record yet. Do not soften that decision on his behalf when it arrives.

### The live feed — the rest of the design

He wants RecCheck reading protel continuously, feeding a shift-aware overlay: arrivals with
times (morning shift shows those landing early enough to reach the desk by 15:30, afternoon
by 22:30 — the `T.` column is the flight time, +2h for transfer), departures beside them
vanishing once their time passes, no-times hidden, and room moves picked up as they happen.

Conclusions reached, so they are not re-litigated:

* **Printing the lists is ruled out.** Already recorded below — a printed list goes stale
  inside the day and the desk corrects it in pen.
* **No live read of protel is zero-touch.** Reading window contents means messages or UI
  Automation. The choice is which mechanism and how much is verified first.
* **Diff the state, do not watch for clicks.** Click-watching only catches actions taken on
  that PC by that user; a colleague or head office voiding a reservation elsewhere produces
  nothing. A state diff catches every change regardless of who made it or how.
* **`SetWinEventHook` out-of-context** is the mechanism for "every window that opens" — the
  callback runs in OUR process, Windows marshals the events, nothing is injected into
  protel. Read once, ~400 ms after `EVENT_OBJECT_SHOW` so the window has finished building.
  Never re-read: read-once-at-open is bounded by human action (~200 windows a shift), where
  polling is not.
* **Watching for a window is zero-touch**; reading its fields is not. `GetWindowText` on
  another process's top-level window returns the cached caption without sending a message.
  So "the date-change dialog opened" is detectable with nothing sent to protel at all.
* **A no-show and a cancellation both vanish from today's arrivals.** They differ in that
  the no-show reappears tomorrow. There is a live void list in protel that would make a
  cancellation a fact rather than a deduction — prefer reading it over inferring.

**Three decisions, answered 03/09. Do not re-open these.**

* **Guest data: tonight only, cleared at 07:00.** Not a snapshot-only design and not a
  ledger-length history — what changed through the shift is kept so he can look back at
  earlier in the night, and it wipes itself at the shift boundary. That is `SHIFT_ROLLOVER_H`,
  the 07:00 clock, **not** the 03:30 working night: the wipe follows the shift being stood,
  the same as the checklist ticks. Whatever holds it must clear on the same event.
* **Record the protel user when protel shows it.** He chose attribution over anonymity,
  knowing it makes the tool a record of what named colleagues did at a shared desk. Asked
  plainly, answered plainly; build it that way and do not soften it on his behalf.

* **The files stay as the fallback.** Asked what happens on a night the live read cannot
  work — protel closed, the window never opened, a list that turns out unreadable — he
  chose keeping the file path rather than retiring it or gating the live read behind a
  confirmation. So the .oxps loading is never removed. the legacy toggle only hides
  the Rate Check slot; it deletes nothing and restores exactly.

Still blocking the field-reading build: **the mechanism decision above.** Answering these was not one — the question
said so in as many words.


### The ledger records; it does not deduce (1.17.9)

Two rules shortened a stay on inference, and both are gone:

* `saveMoves` cut a reservation's departure to the rate list's own date whenever that
  room's arrival key was not in the list — meant to catch an early checkout. It keyed on
  the ARRIVAL, so an arrival that parsed even slightly differently read as "different
  reservation, the old one must be over".
* `ledgerReconcile` ended a stay when the department report showed a room charging under a
  name the tolerant `sameGuest()` did not recognise.

On the night of 30/08 they produced **83 departures against 1 arrival** where protel had
roughly 35 and 26. Rooms 73, 75, 91, 98, 120, 125 and 128 were all cut back to 30/08 — the
list's own business date, which is the signature — while protel had them staying into
September. Verified room by room against protel's In House panel.

Neither was ever asked for. The desk does not care about early checkouts and handles any
difference its own way. **Record the dates the system holds. Do not deduce, correct or
improve them.** That is now rule 4 in `CLAUDE.md`.

**On the arrival dates that once looked wrong:** see the status section above — the
01/09 file parses cleanly, and the inflated arrival count turned out to be room moves,
not misparsed dates.

### Confirmed on the desk

The τ fires on the first press now that the helper sends its own Enter (1.17.3/1.17.4).
The protel-only gate has been pointed at protel and is on. The rate list parser records
correctly. All three were the open questions of this whole run; none of them is open now.
Stop asking about them.

### The τ intermittency was never the shortcut (1.17.3)

He worked it out: he presses Enter immediately after the τ and protel has not finished
reacting, so the Enter is lost. "First press does nothing, second works" was one keypress
arriving too early.

His clincher: *the invoice shortcut always works no matter how fast I press it* — that one
is `seq:`, timed by the helper, so protel is never rushed.

So `tau:<ms>` sends the Enter itself after the layout is back. A malformed `<ms>` is
**rejected outright** rather than degrading to a plain τ. `scratchpad/tauenter.cs` covers
`ParseBind` over ten specs.

Since 1.17.13 the delay is **not user-settable** — see the timing section: it is 1-2% of a
press and was the only knob exposed while the three that mattered were hardcoded.

**The lesson, which cost three releases:** two diagnostics came back perfectly clean and
were read as dead ends. They were the answer. When instrumentation insists everything
worked, believe it and look at what happens *after* the part you instrumented.

### The protel-only gate (1.17.2)

`focus=<needle>` in the binds file: a trigger fires only while the foreground window
matches it on **process name, class or title** — any one, uppercase.

* Blocked presses are **passed through, never swallowed** — the button keeps doing what it
  normally does.
* A window we can read nothing about is **allowed**. A gate that cannot see must never be
  why a shortcut stopped working.
* The needle is never guessed: *Point it at protel* waits 5 s, `rc-tbind fg` prints
  `FG<tab>exe<tab>class<tab>title`, the process name becomes the needle. Off until done.
* The picker refuses to aim at RecCheck itself (`sc-focus-pick` compares against
  `process.execPath`'s last segment, returns `self: true`). Not alt-tabbing in time is the
  likeliest way to break every shortcut.
* Blocked triggers log one line per foreground window — DEBUG → *What the last presses
  did* shows "trigger ignored: protel was not in front" and what was. That is how a wrong
  needle announces itself.
* `MatchesNeedle(exe, cls, txt)` is Win32-free so it can be tested off Windows —
  `scratchpad/focus.cs`, 13 cases. `scratchpad/mainfocus.js` drives the real `main.js`
  with electron stubbed and asserts on the spawn arguments.

**Known simplification:** the decision is cached per `HWND` for the helper's life. A
recycled handle would keep the old answer until the foreground changes. Needs a window to
close and its exact handle reissued; a tick-count bound would close it if it ever matters.

### Depends on a real report

The room movements panel reads the **rate list** — the only report either half sees that
carries arrival and departure dates. It keeps a ledger of reservations (`reccheck_moves_v2`,
`room → arrival → {departure, name, seen}`), so one list answers for every night up to its
last departure, including nights whose tax check was never run. He confirmed the rate list
includes in-house guests leaving that day, so red should fire. **Unconfirmed against a real
file.** Where no list has ever covered a room, a dashed "inferred" pill comes from the guest
name changing.

### The room movements panel — what 1.17.1 was about

The symptom was: rate list loaded, department check opened, no pills. The cause was that
`saveMoves` returned immediately without a business date, and that date came from exactly
one row — page one, matching `/PROTEL HMS/`, with a `dd/mm/yyyy` cell beside it. A list
whose header differs parses its rooms, loads without complaint, and writes not one
reservation.

`parseRate` now tries four things in order: that row, any header row naming protel, the
date printed most often in the file, and the newest arrival in the list itself. Only the
last is a guess and `dateSure` marks it — a guessed date may stamp a reservation but may
never truncate a stay. The rate slot says what went into the ledger at the moment of
loading, and **DEBUG → Why the room pills say what they say** prints the report night,
what the last list recorded, the ledger's span, and every stay within three days of
tonight.

If pills are still missing, that report answers it: either the ledger is empty (the write
half) or the dates do not line up (the read half). Do not guess between them.

### The home screen fits the window (1.17.4)

The rule that makes it work, and the one that broke it twice while getting there:

* **The chain has to be definite the whole way down.** `min-height` gives a flex child
  nothing to shrink *against*, so the column just grows and the overflow moves somewhere
  else. The body itself is the column on this screen (`body.onMenu`), header then page,
  and every link down to `#clList` is flex with `min-height:0`. Only two of the body's
  children are ever in flow here; everything else is `display:none` or fixed.
* **Auto side margins stop a flex item stretching.** `#homeBar` and `#checklist` are both
  `margin:0 auto`, so as flex items they shrank to their contents — 640px down to 383 and
  303 — and stopped lining up with the button below. `width:100%` restores it. Without it
  the bar is only as wide as its buttons and would never wrap however many categories it
  grew, which was half the point.

`#clList` is the only part allowed to scroll, and it says so: `paintListFade()` toggles a
`moreBelow` class (a mask-image fade on the bottom row) on render, on scroll and on
resize. A list that hides tasks with no cue is worse than a long page.

Verified in real Chromium via playwright-core against `/opt/pw-browsers` — jsdom does no
layout, so it cannot answer this question at all. `scratchpad/fit.js` walks six window
sizes × four list lengths asserting the page fits and the audit button is on screen;
`fit2.js` covers the dropdowns (the page can no longer scroll to reveal one that hangs
past the bottom), edit mode, the other screens being untouched, and live resizing;
`fade.js` covers the cue; `cmpshot.js` diffs the box geometry against the previous
release, which is what caught the auto-margin regression.

### Fitting the window is not the same as showing everything (1.17.5)

1.17.4 made the home screen fit the window and I called that done. It was not what was
asked for: a 640px strip down the middle of a 1600px screen still hid twenty tasks behind
a scroll. *"Fit everything in a glance"* meant **use the width**.

`layoutChecklist()` measures the space a column actually has — the gap between the top of
`#clList` and the top of `#auditBtn`, both stable whatever the column count, so it does
not chase its own tail — and flows the list into as many columns as fit, down one column
then across. `--homeW` widens the bar, the card and the audit button together; `--clRows`
sets the column height. One column is exactly the old 640px layout.

Two things worth keeping:

* **The card must never get NARROWER than 640px.** Two columns at 268px each computed to
  594 and the block visibly shrank as tasks were added. Floor it.
* **Don't trust the arithmetic — look.** Rows are not all one height once a title wraps,
  and the row-count sum was off by one often enough to leave a task hidden. After applying
  a column count it now checks `scrollHeight > clientHeight` and takes another column if
  there is width for one. Bounded by the width cap, so it terminates.

The gutter that "Switch user" parks in is reserved against the **window**, not against
`#landing` — the card is centred on screen, so the space either side of it is what counts.
Reserving against `#landing` cost a whole column at 1366px.

### Dialogs used to grow past the window (1.17.5)

`#modal` had no `max-height` and no scroll, and `#modalBg` centres it — so a dialog taller
than the window was cut off at **both** ends with no way to reach either. PROTEL SHORTCUTS
was already 970px tall at 1.17.2, past every window height tested; the focus gate and the
τ box took it to 1192 and the Done button became impossible to reach. Now the modal is
capped to the window and scrolls inside itself, and `.actions` is sticky at the bottom so
the way out of a dialog is never something you have to go looking for.

`#modal:has(.actions){padding-bottom:0}` hands the modal's bottom padding to the sticky
row — without it the row floats 22px up with content still showing underneath.

### Adding a bindable action

Four places, and missing any one of them fails quietly rather than loudly:

1. `app/tbind.cs` — an `ACT_*` constant, a branch in `ParseBind` for the spec token, and a
   branch in `QueueSend`. Alt-combinations go through `SendAltKey(vk)`, which releases
   whatever the user is physically holding first: the trigger may itself be a
   modifier+button combination, and protel would otherwise see Ctrl+Alt+N.
2. `app/bcl-allow.txt` — the new names, then rebuild through `build-helper.sh`.
3. `app/main.js` — `ACTIONS`. This drives the spec built for the command line, and
   `sc-detect` validates against it, so a name missing here cannot be bound at all.
4. `app/index.html` — `SC_ACTIONS` for the row order, plus `sc.<name>` and
   `sc.<name>Why` in **both** language tables.

It is a full installer: the helper and main.js both change.

### A nickname belongs to the guest, not the room (1.17.7)

The nickname is about the **guest**. The department report only lists rooms that were
*charged*, so a room somebody moved out of is simply absent — no event to read.

`syncRooms()`: if the name charging a room is **exactly** the name that carried a nickname
in another room tonight or last night, and that other room is not still charging under it,
the nickname moves with them.

* Exact matching is his call and the right one. `sameGuest()` answers "could this be the
  same person" — correct when shortening a stay, wrong here. `sameName()` trims, collapses
  whitespace, case-folds; none of that can make two different names match.
* **Guard 1:** the old room still charging under that name is one party across two rooms,
  not a move.
* **Guard 2:** two rooms holding the same name is not evidence — an ambiguous match moves
  nothing.
* `tonight` and a `before` snapshot are both built in full before anything is written — a
  room change is a question about two rooms at once.
* `prevNightKey()` decrements through a real Date, so month and year ends work.
* It toasts when it moves a label. That must be visible the night it happens.

**Out of scope, his decision:** a guest who moves and charges nothing in the new room that
night stays invisible until they do charge. Do not build a rate-list path to close it.

### Housekeeping

* A GitHub PAT was pasted in chat early on and **has still not been confirmed revoked** —
  https://github.com/settings/tokens
* The printed corrections sheet has three sections and no totals line. Offered, not taken up.
* The DEBUG menu unlocks by typing `barbarianking` on the home screen, matched on
  `e.code` so it survives a Greek layout.

---

## 9. How he works, which shapes what is worth building

His three working rules now live in `CLAUDE.md` at the repo root, which loads automatically
at the start of a session — this file does not. Read that one first; what follows fills it in.


Night shift, alone, mid-audit while writing here — so answers should be short and the
tool should never need explaining. He reads the code and diagnoses accurately; take his
observations seriously, they have been right more often than my guesses. He asked twice
for verification before shipping after two broken releases in a row, and that stands:
**check it first, and say plainly what has not been checked.**

He speaks Greek and English and has asked for replies in English.

**His four rules now live in `CLAUDE.md`, which loads automatically at the start of a
session — this file does not.** Read that first. Rule 4 arrived last and is the one this
run kept breaking: build what he asked for, nothing beside it; put a better idea to him and
wait, because no answer is not permission. Two features nobody asked for corrupted his
data before that rule existed.

He knows the ground — PIDs versus process names, the DOM, the build — and words prompts
loosely for speed, not from vagueness. Answer the question, not the phrasing, and take the
technical grounding as given: explaining a fundamental back to him is noise. When the
wording is genuinely ambiguous, ask; do not fill the gap with a lecture.
